{
  "name": "station-bills",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "نظام مالي مشترك لدفاتر المحطة — متعدد المستخدمين مع مزامنة ولوحة أدمن",
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "dev": "node --env-file-if-exists=.env --watch server/index.js",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "dependencies": {
    "cookie-session": "^2.1.0",
    "express": "^4.21.2",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "wrangler": "^4.104.0"
  }
}
